# Base URL of the auth API (no trailing slash). In dev this proxies to the
# backend's PORT=4000 default; in production point it at your deployed API,
# e.g. https://api.yourdomain.com/api/auth
VITE_API_BASE_URL=http://localhost:4000/api/auth
